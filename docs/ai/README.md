# AI context system
Keep only resumable facts. `CURRENT_STATE.md` is the compact checkpoint; `WORKFLOW.md` owns cross-model review roles. For real multi-session work add only `docs/ai/work/<feature>/state.md`, `progress.md`, and `implementation-plan.md` unless substantial per-phase detail is unavoidable.
