# Progress

Implemented:
- dedicated filterable Favorites page while preserving the existing Store catalog API
- reusable combined product filter/sort helper
- search + product/kind/rarity/availability/tag + affordability/ownership filters
- active filter count and one-click reset
- intentional signed-out, no-favorites, and no-filter-match states
- favorite removal updates the visible saved set immediately
- product detail, purchase, and gift interactions continue through existing server endpoints
- EN/TR locale copy
- helper and component QUnit regression coverage

Pending:
- open stacked draft PR
- exact-head Official Discourse Plugin CI
- fix only failures attributable to this branch
- keep unmerged until explicit authorization
