@AGENTS.md

# Gemini adapter
Treat `AGENTS.md` as canonical. Route only to local instructions/source relevant to the active provider/store/payment surface. Do not recursively preload every local file. Cross-model procedures live under `.agents/skills/`.
